## Packages
date-fns | Date formatting and age calculation
lucide-react | High quality icons
clsx | Utility for constructing className strings conditionally
tailwind-merge | Utility to merge tailwind classes
react-hook-form | Form state management
@hookform/resolvers | Zod resolver for react-hook-form

## Notes
- Sidebar requires a layout wrapper that avoids rendering on the Print page.
- Print prescription layout uses `@media print` to hide navigation and format to A5/Letter size.
- Forms use `@shared/schema` definitions for validation.
